// Auth client-side utilities
// Import signIn, signOut, and useSession from next-auth/react in client components

export { signIn, signOut, useSession, SessionProvider } from "next-auth/react";
export { auth } from "@/lib/auth";
